# Step 50 Implement Pass 2

Inputs
- run_id: {{ .TaskPackage.RunID }}
- pass: {{ .Pass }}
- agent: {{ .Agent }}
- worktree_path: {{ .WorktreePath }}
- experiment_lesson_ids: {{ .CandidateRuleIDs }}

Task Package
- title: {{ .TaskPackage.Title }}
- base_sha: {{ .TaskPackage.BaseSHA }}
- best_branch: {{ .TaskPackage.BestBranch }}

Task Brief
{{ .TaskPackage.ReconstructedTaskPrompt }}

Pass 2 requirements
- Implement the task brief in this worktree.
- Modify repository files only.
- Write `checklist-result.json` at the worktree root.
- Follow the current learned rules listed below.
- Treat every experiment lesson below as an additional learned-policy constraint for pass2.

Current Learned Rules
{{- if .ActiveRules }}
{{ range .ActiveRules }}
## {{ .RuleID }}
- path: {{ .RulePath }}

{{ .Body }}

{{ end }}
{{- else }}
- none
{{- end }}

Experiment Lessons
{{- if .RulePayloads }}
{{ range .RulePayloads }}
## {{ .ID }}
- kind: {{ .Kind }}
- target_rule_id: {{ if .TargetRuleID }}{{ .TargetRuleID }}{{ else }}(none){{ end }}
- title: {{ .Title }}

{{ .ProposedBody }}

{{ end }}
{{- else }}
- none
{{- end }}

Experiment Checklist
- The lesson bodies above are the source of truth.
- Use their `Checklist Item` sections as the pass2 checklist.
- For `checklist-result.json`, use the experiment_lesson_ids shown in Inputs as `rule_id` values.

Expected agent-owned artifact
- checklist-result.json (write this at the worktree root)

Checklist artifact contract
- `checklist-result.json` must be strict JSON.
- Top-level keys must be exactly:
  - `schema_version`
  - `run_id`
  - `pass`
  - `agent`
  - `items`
- Each `items[]` object must use this schema:
  - `rule_id`: required string
  - `verdict`: required string, one of `compliant`, `n_a`, or `exception`
  - `rationale`: optional string for `compliant`/`n_a`; required and non-empty when `verdict` is `exception`
  - `exception_reason`: optional string with extra detail when `verdict` is `exception`
- Do not use item keys like `id`, `status`, `result`, `description`, `file`, or `files`.
- Use `items: []` when no learned rule or checklist item applies.
- Use this shape:
```json
{"schema_version":"1","run_id":"{{ .TaskPackage.RunID }}","pass":{{ .Pass }},"agent":"{{ .Agent }}","items":[{"rule_id":"task-scope","verdict":"compliant","rationale":"Implemented the task scope."}]}
```
- Do not add keys like `status`, `completed_checks`, or markdown wrappers.

Harness-owned artifacts
- The harness captures `session.jsonl`.
- The harness computes `diff.patch`.
- The harness writes `manifest.json`.
- Do not create or overwrite `manifest.json`, `session.jsonl`, or `diff.patch` yourself.
