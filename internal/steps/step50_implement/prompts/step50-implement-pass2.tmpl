# Step 50 Implement Pass 2

Inputs
- run_id: {{ .TaskPackage.RunID }}
- pass: {{ .Pass }}
- agent: {{ .Agent }}
- worktree_path: {{ .WorktreePath }}
- candidate_rule_ids: {{ .CandidateRuleIDs }}

Task Package
- title: {{ .TaskPackage.Title }}
- base_sha: {{ .TaskPackage.BaseSHA }}
- best_branch: {{ .TaskPackage.BestBranch }}

Task Brief
{{ .TaskPackage.ReconstructedTaskPrompt }}

Pass 2 requirements
- Implement the task brief in this worktree.
- Modify repository files only.
- Treat every candidate rule below as an additional required constraint for pass2.
- If a candidate rule calls out a missing companion edit, missing file sync, or another concrete gap from pass1, make the corresponding code/file changes in this pass.
- Do not stop at pass1 parity when a candidate rule identifies work that is still missing.
- Before finishing, read each candidate's `Problem`, `Evidence`, and `Proposed rule` sections and make sure those pass1 failure statements are no longer true of your pass2 diff.
- A pass2 output that repeats the same violated condition from pass1 is incorrect, even if it satisfies the original task brief.

Candidate Rules
{{- if .RulePayloads }}
{{ range .RulePayloads }}
## {{ .ID }}
- kind: {{ .Kind }}
- target_rule_id: {{ if .TargetRuleID }}{{ .TargetRuleID }}{{ else }}(none){{ end }}
- title: {{ .Title }}

{{ .ProposedBody }}

{{ end }}
{{- else }}
- none
{{- end }}

Expected agent-owned artifact
- checklist-result.json

Checklist artifact contract
- `checklist-result.json` must be strict JSON with exactly these top-level keys:
  - `schema_version`
  - `run_id`
  - `pass`
  - `agent`
  - `items`
- Use this shape:
```json
{"schema_version":"1","run_id":"{{ .TaskPackage.RunID }}","pass":{{ .Pass }},"agent":"{{ .Agent }}","items":[]}
```
- Do not add keys like `status`, `completed_checks`, or markdown wrappers.

Harness-owned artifacts
- The harness captures `session.jsonl`.
- The harness computes `diff.patch`.
- The harness writes `manifest.json`.
- Do not create or overwrite `manifest.json`, `session.jsonl`, or `diff.patch` yourself.
